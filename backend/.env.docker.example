# ---------------------------------------------------------------------------
# Backend environment variables for Docker deployments
# Copy to backend/.env.docker and replace placeholder values before running.
# ---------------------------------------------------------------------------

# Core application settings
PROJECT_NAME=Eleva
VERSION=1.0.0
API_V1_STR=/api/v1

# Security configuration
SECRET_KEY=replace-with-strong-secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database connection (matches docker-compose defaults)
DATABASE_URL=postgresql+asyncpg://eleva:eleva@db:5432/eleva

# Allowed web origins (hostnames that can reach the API from the browser)
BACKEND_CORS_ORIGINS=["http://localhost:8080", "http://127.0.0.1:8080"]

# Email provider configuration (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# OpenAI configuration (required for AI features)
OPENAI_API_KEY=
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=1536

# AWS S3 configuration (optional, used for profile pictures)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=
AWS_REGION=eu-north-1

# Document processing defaults
MAX_FILE_SIZE_MB=50
CHUNK_SIZE=2500
CHUNK_OVERLAP=250
CHUNKING_STRATEGY=sentence
