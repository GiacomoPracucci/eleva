# ---------------------------------------------------------------------------
# Frontend environment variables for Docker builds
# Copy to frontend/.env.docker if you want to override the defaults.
# Values are consumed during `docker compose build`.
# ---------------------------------------------------------------------------
VITE_API_URL=/api/v1
VITE_APP_NAME=Eleva
VITE_APP_VERSION=1.0.0
VITE_ENABLE_PROFILE_UPLOAD=true
VITE_ENABLE_AI_FEATURES=false
