eval $(poetry env activate) # attivare ambiente virtuale poetry
npm run dev # lanciare server vite
mkdir -p components/common components/auth components/subjects components/layout
mkdir -p pages services hooks store types utils contexts